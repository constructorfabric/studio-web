# Tilde forms (subscript falls back to rawInline)

Deleted text looks like ~~this~~, while H~2~O uses a single tilde on each
side for a subscript, which this engine currently preserves verbatim rather
than rendering, pending a subscript mark in the editor schema.
