{
  "$id": "gts://gts.cf.studio.rel.artifact_of.v1~",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "rel:artifact_of",
  "description": "Typed edge (artifact_of); the relation is the GTS type, endpoints are instance ids.",
  "type": "object",
  "properties": {
    "from": { "type": "string", "description": "source instance id" },
    "to":   { "type": "string", "description": "target instance id" }
  },
  "required": ["from", "to"],
  "additionalProperties": false
}
