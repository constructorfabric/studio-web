{
  "$id": "gts://gts.cf.studio.rel.references.v1~",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "rel:references",
  "description": "Typed edge (references); the relation is the GTS type, endpoints are instance ids.",
  "type": "object",
  "properties": {
    "from": { "type": "string", "description": "source instance id" },
    "to":   { "type": "string", "description": "target instance id" }
  },
  "required": ["from", "to"],
  "additionalProperties": false
}
