# Constructor Studio Backend — dev configuration (SQLite, static auth).
# Layered config: this YAML -> env APP__* -> CLI. Unknown fields inside a
# gear `config:` section fail that gear's init loudly — that is intended.
#
# Verify the effective assembly before first run:
#   cargo run -- --config config/dev.yaml --list-gears
#   cargo run -- --config config/dev.yaml --dump-gears-config

server:
  home_dir: "~/.cf-studio-backend"

database:
  servers:
    sqlite_main:
      engine: "sqlite"
      params:
        WAL: "true"
        synchronous: "NORMAL"
        busy_timeout: "5000"
      pool:
        max_conns: 1          # SQLite is single-writer
        acquire_timeout: "30s"

    # PostgreSQL 19 + pgvector, used by graph-storage and nothing else. That
    # gear's migrations need `CREATE EXTENSION vector`, `CREATE PROPERTY GRAPH`
    # (SQL/PGQ, PostgreSQL 19+) and an HNSW index, none of which the server
    # above provides. Routing one gear here keeps every other gear on the
    # stable engine — see the `graph-postgres` service in docker-compose.yml.
    pg_graph:
      engine: "postgres"
      host: "127.0.0.1"
      port: 5433
      user: "graph"
      password: "${STUDIO_GRAPH_PG_PASSWORD:-graph}"
      dbname: "graph_storage"
      pool:
        max_conns: 10
        acquire_timeout: "30s"

logging:
  default:
    console_level: info
    file: "logs/studio-backend.log"
    file_level: info
  account_management:
    console_level: debug
    file: "logs/account-management.log"
    file_level: debug

gears:
  # Wrapper over the external spec-quality detector service (bloat / purpose /
  # leak / traceability). The service has its OWN bearer secret; it lives here
  # (env STUDIO_SPEC_QUALITY_API_KEY) and never reaches the browser. Unset key
  # = analysis calls 500 with a clear message; the backend still boots.
  studio-spec-quality:
    config:
      base_url: "https://349ddd50986c491c844d92fdaefcadfe.constructor.pro"
      api_key_env: "STUDIO_SPEC_QUALITY_API_KEY"

  api-gateway:
    config:
      bind_addr: "127.0.0.1:8090"
      prefix_path: "/cf"
      enable_docs: true        # OpenAPI browser at /cf/docs — source of truth for paths
      cors_enabled: false
      openapi:
        title: "Constructor Studio Backend (dev)"
        version: "0.1.0"
        description: "Studio backend assembly — account-management demo"

  gear-orchestrator:
    config: {}

  grpc-hub:
    config:
      listen_addr: "uds:///tmp/cf-studio-backend-grpc"

  # ── AuthN: static bearer tokens for the demo ──
  authn-resolver:
    config:
      vendor: "constructorfabric"

  static-authn-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100
      mode: static_tokens
      tokens:
        # Platform admin: home tenant = root tenant (see account-management.bootstrap.root_id)
        - token: "studio-admin-token"
          identity:
            subject_id: "00000000-0000-0000-0000-00000000a001"
            subject_tenant_id: "00000000-0000-0000-0000-000000000001"
            token_scopes: ["*"]
            subject_type: "gts.cf.core.security.subject_user.v1~"
        # Regular user for the frontend login demo: subject = the deterministic
        # UUIDv5 the echo IdP mints for username "a.kuchma" (demo.sh step 6).
        - token: "studio-user-token"
          identity:
            subject_id: "ac6ae8e2-b765-541b-8ecd-405addfaef21"
            subject_tenant_id: "00000000-0000-0000-0000-000000000001"
            token_scopes: ["*"]
            subject_type: "gts.cf.core.security.subject_user.v1~"
      # S2S: mini-chat exchanges these for a SecurityContext to call other gears
      s2s_credentials:
        # Explicit identity: without it the plugin's built-in default tenant id
        # is used, which does not exist in OUR tenant tree -> the AM tenant-
        # resolver plugin fails OAGW hierarchy resolution (TenantNotFound).
        - client_id: "mini-chat"
          client_secret: "mini-chat-dev-secret"
          identity:
            subject_id: "00000000-0000-0000-0000-00000000b001"
            subject_tenant_id: "00000000-0000-0000-0000-000000000001"
            token_scopes: ["*"]
            subject_type: "gts.cf.core.security.subject_user.v1~"

  # ── AuthZ: permissive static PDP for the demo (Studio roles later = own PDP plugin) ──
  authz-resolver:
    config:
      vendor: "constructorfabric"

  static-authz-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  tenant-resolver:
    config:
      vendor: "constructorfabric"

  types-registry:
    database:
      server: "sqlite_main"
      file: "types_registry.db"
    config:
      # Seed Studio tenant types at startup (solves bootstrap chicken-and-egg:
      # AM validates root_tenant_type against types-registry before any REST is up).
      entities:
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.core.am.platform.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Platform root tenant type (root-only)"
          type: "object"
          x-gts-traits:
            allowed_parent_types: []
            idp_provisioning: false
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.studio.tenant.organization.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio organization tenant"
          type: "object"
          x-gts-traits:
            allowed_parent_types: ["gts.cf.core.am.tenant_type.v1~cf.core.am.platform.v1~"]
            idp_provisioning: false
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.studio.tenant.workspace.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio workspace tenant (product-line context)"
          type: "object"
          x-gts-traits:
            allowed_parent_types: ["gts.cf.core.am.tenant_type.v1~cf.studio.tenant.organization.v1~"]
            idp_provisioning: false
        # Studio project tenant: a project is its own codebase context (sources,
        # IDE session, artifact graph, spec-quality) — all of which are already
        # tenant-scoped, so the id of this tenant is what the session/ingest/
        # settings paths key on.
        #
        # Two allowed parents, not one. The portal's New project wizard has no
        # workspace field (ADR-0010) and creates under the organization the
        # session is in; workspace stays allowed so the Org → Workspace →
        # Project shape remains available and already-seeded trees keep working.
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.studio.tenant.project.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio project tenant (a codebase context inside an organization or a workspace)"
          type: "object"
          x-gts-traits:
            allowed_parent_types:
              - "gts.cf.core.am.tenant_type.v1~cf.studio.tenant.organization.v1~"
              - "gts.cf.core.am.tenant_type.v1~cf.studio.tenant.workspace.v1~"
            idp_provisioning: false
        # Workspace settings as AM tenant metadata (vision J2: automation level,
        # approved worker categories). override_only: each workspace sets its own.
        # Connector driver contract (see oidc.yaml for the rationale).
        - "$id": "gts://gts.cf.toolkit.plugins.plugin.v1~cf.studio.connector.plugin.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio source connector provider plugin specification"
          type: "object"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.connector.catalogue.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio source connections (free-form until gears issue #4)"
          type: "object"
          x-gts-traits:
            # inherit: a workspace sees its organization's connections.
            # The nearest row wins whole (AM does not merge across the
            # chain), so a tenant with its own catalogue shadows its
            # ancestors rather than adding to them.
            inheritance_policy: "inherit"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.workspace.settings.v1~"
          # Free-form payload: the GTS OP#12 narrowing check treats the base
          # tenant_metadata envelope as a CLOSED empty object, so a derived
          # schema may not declare typed properties at all (gears issue #4).
          # Shape {automation_level, approved_worker_categories} is enforced
          # client-side until the base schema is relaxed upstream.
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio workspace settings (free-form until gears issue #4)"
          type: "object"
          x-gts-traits:
            inheritance_policy: "override_only"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.project.config.v1~"
          # Project attributes on the project tenant: {mode, stages[], status,
          # source}. Free-form for the same gears issue #4 reason as the settings
          # above; shape enforced client-side. Per-tenant (not inherited) —
          # a project must not read its parent's attributes as its own.
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio project attributes (mode, stages, status, source) — free-form until gears issue #4"
          type: "object"
          x-gts-traits:
            inheritance_policy: "override_only"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.access.config.v1~"
          # Organization access config (ADR-0006): {model, roles[]}. Free-form
          # object like workspace.settings; shape enforced client-side until the
          # base tenant_metadata schema is relaxed upstream (gears issue #4).
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio organization access model + roles (free-form)"
          type: "object"
          x-gts-traits:
            inheritance_policy: "inherit"

  nodes-registry:
    database:
      server: "sqlite_main"
      file: "nodes_registry.db"
    config: {}

  resource-group:
    database:
      server: "sqlite_main"
      file: "resource_group.db"
    config: {}

  # ── The gear under demonstration ──
  account-management:
    database:
      server: "sqlite_main"
      file: "account_management.db"
    config:
      bootstrap:
        # Deployment-stable ids — do NOT change between restarts (idempotency contract)
        root_id: "00000000-0000-0000-0000-000000000001"
        root_name: "Constructor Platform"
        root_tenant_type: "gts.cf.core.am.tenant_type.v1~cf.core.am.platform.v1~"
        strict: true
      # AM's co-located tenant-resolver plugin (barrier-aware reads over tenant_closure)
      tr_plugin:
        enabled: true
      # listing / hierarchy / retention / reaper / idp — defaults are fine for dev
      # (see gears-rust/gears/system/account-management/docs/config-example.yaml)

  static-idp-plugin:
    config:
      vendor: "cf"       # matches AM idp.vendor default
      priority: 100

  # The official keycloak-idp-plugin is linked in the binary but has no Keycloak
  # to reach in this profile — keep it dormant so its init doesn't try (and fail)
  # to pre-warm an admin token. static-idp (echo, vendor "cf") above stays the
  # active IdP here.
  keycloak-idp-plugin:
    config:
      enabled: false

  # ── Feature gears: per-user settings, files, AI chat ──

  simple-user-settings:
    database:
      server: "sqlite_main"
      file: "settings.db"
    config:
      max_field_length: 200

  file-storage:
    database:
      server: "sqlite_main"
      file: "file_storage.db"
    config:
      # Dev-only Ed25519 seed so signed URLs survive restarts (generate your
      # own for any shared environment).
      signing_key_seed: "ej5FEOW2E1emMKbKUNBKALhdHb-VLvLNM-n4FcuDREY"

  # LLM egress chain: mini-chat -> oagw -> provider; secrets via credstore
  oagw:
    config:
      proxy_timeout_secs: 120

  credstore:
    database:
      server: "sqlite_main"
      file: "credstore.db"
    config:
      vendor: "constructorfabric"

  # No studio-credstore-pg section in this profile on purpose. The seeds below
  # are real VALUES read from this file, and the persistent value store
  # (issue #66) would win the vendor selection and orphan them — this profile
  # has no studio-secrets-bootstrap to re-supply them from the environment.
  # The profiles that do enable it (docker/oidc/k8s) get their values that way.
  static-credstore-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100
      secrets:
        # Shared secret (no tenant_id). Put your real OpenAI key here for the
        # Ask AI feature; without it chats are created but streaming replies
        # fail at the provider with 401.
        - key: "openai-key"
          value: "sk-REPLACE_ME"

  mini-chat:
    database:
      server: "sqlite_main"
      file: "mini_chat.db"
    config:
      outbox:
        num_partitions: 2   # SQLite is single-writer; default 4 causes contention
      client_credentials:
        client_id: "mini-chat"
        client_secret: "mini-chat-dev-secret"
      providers:
        openai:
          kind: openai_responses
          storage_kind: openai
          host: "api.openai.com"
          auth_plugin_type: "gts.cf.core.oagw.auth_plugin.v1~cf.core.oagw.apikey.v1"
          auth_config:
            header: "Authorization"
            prefix: "Bearer "
            secret_ref: "openai-key"

  static-mini-chat-model-policy-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100
      model_catalog:
        - id: "gpt-5.2"
          provider_model_id: "gpt-5.2"
          display_name: "GPT-5.2"
          tier: Standard
          enabled: true
          system_prompt: "You are the Constructor Studio workspace assistant. Be concise and practical."
          thread_summary_prompt: ""
          input_tokens_credit_multiplier_micro: 1000000
          output_tokens_credit_multiplier_micro: 3000000
          multimodal_capabilities: ["VISION_INPUT"]
          context_window: 128000
          max_output_tokens: 8192
          max_input_tokens: 120000
          description: "Default Studio assistant model"
          version: "2025-01"
          icon: ""
          provider_display_name: "OpenAI"
          multiplier_display: "1x"
          provider_id: "openai"
          max_num_results: 10
          max_tool_calls: 2
          estimation_budgets:
            bytes_per_token_conservative: 4
            fixed_overhead_tokens: 500
            safety_margin_pct: 10
            image_token_budget: 1000
            tool_surcharge_tokens: 200
            web_search_surcharge_tokens: 500
            code_interpreter_surcharge_tokens: 1000
            minimal_generation_floor: 100
          preference:
            is_default: true
            sort_order: 1
          general_config:
            type: "chat"
            available_from: "2025-01-01T00:00:00Z"
            max_file_size_mb: 50
            api_params:
              temperature: 0.7
              top_p: 1.0
              frequency_penalty: 0.0
              presence_penalty: 0.0
              stop: []
            features:
              streaming: true
              structured_output: true
            tool_support:
              web_search: false
              file_search: false
              image_generation: false
              code_interpreter: false
              mcp: false
            supported_endpoints:
              chat_completions: true
              responses: true
              embeddings: false
              image_generation: false
              audio_speech_generation: false
              audio_transcription: false
              audio_translation: false

  # ── Studio's own gear: per-workspace Theia IDE sessions in containers ──
  # Requires a Docker daemon; the CI-published image is pulled/refreshed
  # automatically (docker login ghcr.io once if the package is private).
  # Projects (ADR-0005 studio-project gear) retired: a project is now an AM
  # tenant (type cf.studio.tenant.project.v1~) with its attributes in the
  # cf.studio.project.config.v1~ tenant metadata — no separate project gear.

  studio-session:
    config:
      image: "ghcr.io/constructorfabric/studio-web/cf-studio-theia:edge"
      always_pull: true
      workspaces_root: "~/.cf-studio-workspaces"
      bind_host: "127.0.0.1"      # loopback only — the Theia PoC has no auth
      public_host: "localhost"
      port_range_start: 41000
      port_range_end: 41099
      max_session_secs: 14400     # 4h, then the reaper stops the container
      git_mode: "disabled"

  # oidc-authn-plugin is link-time active in every profile (gears discover
  # via inventory), and it REQUIRES a config section. In this static-token
  # profile it is configured valid but DEPRIORITIZED: static-authn-plugin
  # (priority 100) wins the vendor selection, so this section is inert.
  oidc-authn-plugin:
    config:
      vendor: "constructorfabric"
      priority: 1000   # LOWER number wins vendor selection; static (100) stays in charge here
      jwt:
        supported_algorithms: ["RS256", "ES256"]
        require_audience: false
        trusted_issuers:
          - issuer: "https://localhost:8443/realms/studio"
        claim_mapping:
          subject_id: "sub"
          subject_tenant_id: "tenant_id"
          token_scopes: "scope"
      s2s_oauth:
        discovery_url: "https://localhost:8443/realms/studio"
        default_subject_type: "gts.cf.core.security.subject_service.v1~"

  # Knowledge graph: typed multi-tenant nodes and edges, bounded traversal and
  # hybrid retrieval. Pinned to `pg_graph` (PostgreSQL 19) by the block above;
  # it cannot run on the main server.
  graph-storage:
    database:
      server: "pg_graph"
      dbname: "graph_storage"
    config:
      # `pgq` runs each hop as a single SQL/PGQ `GRAPH_TABLE` statement. A
      # request whose scope cannot be bounded to a set of tenants is served by
      # `two_query` instead, so this is safe to leave on; `two_query` is the
      # portable fallback and `cte` is deliberately absent while the pinned
      # gears-rust revision predates the safe-CTE API it needs.
      traversal_hop: pgq
