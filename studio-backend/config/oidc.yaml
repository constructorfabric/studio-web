# Constructor Studio Backend — OIDC profile (real login via Keycloak).
# postgres.yaml with static-authn-plugin replaced by oidc-authn-plugin.
# Start the IdP first:  docker compose up -d graph-postgres keycloak
# Portal login: "Sign in with SSO" (admin/studio or demo/studio).
# For an EXTERNAL IdP see README "OIDC login" — you need an issuer URL, a
# public PKCE client, UUID `sub`, and a `tenant_id` claim (home tenant UUID).
server:
  home_dir: "~/.cf-studio-backend"

database:
  # NB: this does NOT create missing Postgres databases, despite what it sounds
  # like — toolkit-db only consults it to create SQLite parent directories
  # (manager.rs, finalize_sqlite_paths). Postgres databases come from
  # docker/initdb/01-create-databases.sql, which runs once on an empty volume.
  auto_provision: true
  servers:
    # Variant B, as in docker.yaml: ONE Postgres instance for the whole
    # assembly — the `graph-postgres` service (PostgreSQL 19 + pgvector), which
    # hosts every gear database as well as graph_storage. The former
    # postgres:16 service ("studio-backend-pg") that used to answer on 5432 was
    # removed from docker-compose.yml; pointing here at 5432 leaves the backend
    # depending on a container compose no longer knows how to start.
    #
    # 5433 rather than 5432 because this profile runs the backend on the HOST:
    # the container listens on 5432 and compose publishes it as
    # 127.0.0.1:5433. In-network profiles use `graph-postgres:5432` instead.
    pg_main:
      engine: "postgres"
      host: "127.0.0.1"
      port: 5433
      user: "studio"
      password: "${STUDIO_PG_PASSWORD:-studio}"
      dbname: "studio"            # admin/default db; gears override per-gear below
      pool:
        max_conns: 10
        acquire_timeout: "30s"

    # Same physical instance as pg_main; the graph-storage gear keeps its own
    # `server: pg_graph` and its `graph_storage` database, where the vector /
    # PROPERTY GRAPH / HNSW migrations run. The credentials are pg_main's: the
    # instance is created with POSTGRES_USER=studio and has no `graph` role.
    pg_graph:
      engine: "postgres"
      host: "127.0.0.1"
      port: 5433
      user: "studio"
      password: "${STUDIO_PG_PASSWORD:-studio}"
      dbname: "graph_storage"
      pool:
        max_conns: 10
        acquire_timeout: "30s"

logging:
  default:
    console_level: info
    file: "logs/studio-backend.log"
    file_level: info
  account_management:
    console_level: debug
    file: "logs/account-management.log"
    file_level: debug

gears:
  # Wrapper over the external spec-quality detector service (bloat / purpose /
  # leak / traceability). The service has its OWN bearer secret; it lives here
  # (env STUDIO_SPEC_QUALITY_API_KEY) and never reaches the browser. Unset key
  # = analysis calls 500 with a clear message; the backend still boots.
  studio-spec-quality:
    config:
      base_url: "https://349ddd50986c491c844d92fdaefcadfe.constructor.pro"
      api_key_env: "STUDIO_SPEC_QUALITY_API_KEY"

  api-gateway:
    config:
      # 0.0.0.0: session containers reach the gateway via
      # host.docker.internal (the gate's /studio-api/* bridge); auth is
      # enforced by the oidc plugin on every request either way.
      bind_addr: "0.0.0.0:8090"
      prefix_path: "/cf"
      enable_docs: true
      cors_enabled: false
      openapi:
        title: "Constructor Studio Backend (postgres)"
        version: "0.1.0"
        description: "Studio backend assembly — account-management demo"

  gear-orchestrator:
    config: {}

  grpc-hub:
    config:
      listen_addr: "uds:///tmp/cf-studio-backend-grpc"

  authn-resolver:
    config:
      vendor: "constructorfabric"

  # Real login: validates JWTs issued by the Keycloak from docker-compose
  # (docker compose up -d keycloak). One authn plugin per vendor wins by
  # priority — this profile simply does not configure static-authn-plugin
  # (linked but inactive). The plugin enforces https for IdP endpoints, so
  # dev Keycloak runs TLS with a self-signed cert; its CA is trusted below.
  oidc-authn-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100
      jwt:
        supported_algorithms: ["RS256", "ES256"]
        require_audience: false
        trusted_issuers:
          - issuer: "https://localhost:8443/realms/studio"
        claim_mapping:
          subject_id: "sub"              # Keycloak user id (UUID)
          subject_tenant_id: "tenant_id" # user attribute -> claim (realm import)
          token_scopes: "scope"
        first_party_clients:
          - "studio-portal"
      http_client:
        custom_ca_certificate_paths:
          - "../docker/keycloak/certs/dev-ca.pem"   # relative to studio-backend cwd
      s2s_oauth:
        # mini-chat exchanges client credentials against the same realm
        # (confidential client "mini-chat", secret mini-chat-dev-secret,
        # hardcoded tenant_id claim — see docker/keycloak/realm-studio.json).
        discovery_url: "https://localhost:8443/realms/studio"
        default_subject_type: "gts.cf.core.security.subject_service.v1~"

  authz-resolver:
    config:
      vendor: "constructorfabric"

  static-authz-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  tenant-resolver:
    config:
      vendor: "constructorfabric"

  types-registry:
    database:
      server: "pg_main"
      dbname: "studio_types_registry"
    config:
      entities:
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.core.am.platform.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Platform root tenant type (root-only)"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_type.v1~"
          x-gts-traits:
            allowed_parent_types: []
            idp_provisioning: false
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.studio.tenant.organization.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio organization tenant"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_type.v1~"
          x-gts-traits:
            allowed_parent_types: ["gts.cf.core.am.tenant_type.v1~cf.core.am.platform.v1~"]
            idp_provisioning: false
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.studio.tenant.workspace.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio workspace tenant (product-line context)"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_type.v1~"
          x-gts-traits:
            allowed_parent_types: ["gts.cf.core.am.tenant_type.v1~cf.studio.tenant.organization.v1~"]
            idp_provisioning: false
        # Studio project tenant: a project is its own codebase context (sources,
        # IDE session, artifact graph, spec-quality) — all of which are already
        # tenant-scoped, so the id of this tenant is what the session/ingest/
        # settings paths key on.
        #
        # Two allowed parents, not one. The portal's New project wizard has no
        # workspace field (ADR-0010) and creates under the organization the
        # session is in; workspace stays allowed so the Org → Workspace →
        # Project shape remains available and already-seeded trees keep working.
        - "$id": "gts://gts.cf.core.am.tenant_type.v1~cf.studio.tenant.project.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio project tenant (a codebase context inside an organization or a workspace)"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_type.v1~"
          x-gts-traits:
            allowed_parent_types:
              - "gts.cf.core.am.tenant_type.v1~cf.studio.tenant.organization.v1~"
              - "gts.cf.core.am.tenant_type.v1~cf.studio.tenant.workspace.v1~"
            idp_provisioning: false
        # Connector driver contract. Each driver plugin registers a
        # PluginV1 instance derived from this type, so `GET
        # /studio-connector/v1/providers` is really "what is in the
        # types registry", not a hardcoded list.
        - "$id": "gts://gts.cf.toolkit.plugins.plugin.v1~cf.studio.connector.plugin.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio source connector provider plugin specification"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.toolkit.plugins.plugin.v1~"
        # Connection catalogue of one tenant. Tokens are NOT here — only
        # credstore references. inheritance_policy: inherit lets a
        # workspace see its organization's connections; per AM contract
        # the nearest row wins whole, it does not merge with ancestors.
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.connector.catalogue.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio source connections (free-form until gears issue #4)"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_metadata.v1~"
          x-gts-traits:
            # inherit: a workspace sees its organization's connections.
            # The nearest row wins whole (AM does not merge across the
            # chain), so a tenant with its own catalogue shadows its
            # ancestors rather than adding to them.
            inheritance_policy: "inherit"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.workspace.settings.v1~"
          # Free-form payload: the GTS OP#12 narrowing check treats the base
          # tenant_metadata envelope as a CLOSED empty object, so a derived
          # schema may not declare typed properties at all (gears issue #4).
          # Shape {automation_level, approved_worker_categories} is enforced
          # client-side until the base schema is relaxed upstream.
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio workspace settings (free-form until gears issue #4)"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_metadata.v1~"
          x-gts-traits:
            inheritance_policy: "override_only"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.project.config.v1~"
          # Project attributes on the project tenant: {mode, stages[], status,
          # source}. Free-form for the same gears issue #4 reason as the settings
          # above; shape enforced client-side. Per-tenant (not inherited) —
          # a project must not read its parent's attributes as its own.
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio project attributes (mode, stages, status, source) — free-form until gears issue #4"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_metadata.v1~"
          x-gts-traits:
            inheritance_policy: "override_only"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.project.kit_installations.v1~"
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Desired Constructor Studio kit installations for one project"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_metadata.v1~"
          x-gts-traits:
            inheritance_policy: "override_only"
        - "$id": "gts://gts.cf.core.am.tenant_metadata.v1~cf.studio.access.config.v1~"
          # Organization access config (ADR-0006): {model, roles[]}. Free-form
          # object like workspace.settings; shape enforced client-side until the
          # base tenant_metadata schema is relaxed upstream (gears issue #4).
          "$schema": "http://json-schema.org/draft-07/schema#"
          description: "Studio organization access model + roles (free-form)"
          type: "object"
          # Derived types must compose their base (gts 0.12 inclusion check):
          # the base is a closed envelope with an open `payload` container.
          allOf:
            - "$ref": "gts://gts.cf.core.am.tenant_metadata.v1~"
          x-gts-traits:
            inheritance_policy: "inherit"

  nodes-registry:
    database:
      server: "pg_main"
      dbname: "studio_nodes_registry"
    config: {}

  resource-group:
    database:
      server: "pg_main"
      dbname: "studio_resource_group"
    config: {}

  account-management:
    database:
      server: "pg_main"
      dbname: "studio_account_management"
    config:
      bootstrap:
        root_id: "00000000-0000-0000-0000-000000000001"
        root_name: "Constructor Platform"
        root_tenant_type: "gts.cf.core.am.tenant_type.v1~cf.core.am.platform.v1~"
        strict: true
        # Bind the platform root to the shared `studio` realm; descendants inherit
        # it via parent_context (full rationale in docker.yaml). Needs a fresh DB.
        root_tenant_metadata:
          realm_name: "studio"
      # Select the official Keycloak IdP plugin.
      idp:
        vendor: "keycloak"
      tr_plugin:
        enabled: true

  # Official cf-gears-keycloak-idp-plugin — real user provisioning (invites) for
  # the OIDC dev profile. The backend here runs on the HOST (WSL), so it reaches
  # Keycloak's ADMIN API over plain HTTP on the spare published port 8088
  # (compose maps 127.0.0.1:8088 -> keycloak:8080). That sidesteps the self-signed
  # TLS on :8443 — which the official plugin can only trust via a CredStore CA
  # ref, not a file path. Browser OIDC login is unaffected (still https://...:8443).
  # static-idp stays linked but unconfigured here (idle); this is the active IdP.
  keycloak-idp-plugin:
    config:
      vendor: "keycloak"
      priority: 50
      security_context:
        platform_tenant_id: "00000000-0000-0000-0000-000000000001"
      keycloak:
        base_url: "http://localhost:8088"
        bootstrap_admin:
          realm: "studio"
          client_id: "studio-admin"
          client_secret: "studio-admin-dev-secret"
        realm_admin:
          client_id_default: "studio-admin"
          default_shared_realm_secret: "studio-admin-dev-secret"
      tenant_facade: {}
      user_facade: {}

  # ── Feature gears: per-user settings, files, AI chat ──

  simple-user-settings:
    database:
      server: "pg_main"
      dbname: "studio_settings"
    config:
      max_field_length: 200

  file-storage:
    database:
      server: "pg_main"
      dbname: "studio_file_storage"
    config:
      # Dev-only Ed25519 seed so signed URLs survive restarts (generate your
      # own for any shared environment).
      signing_key_seed: "ej5FEOW2E1emMKbKUNBKALhdHb-VLvLNM-n4FcuDREY"

  # LLM egress chain: mini-chat -> oagw -> provider; secrets via credstore
  oagw:
    config:
      proxy_timeout_secs: 120

  credstore:
    database:
      server: "pg_main"
      dbname: "studio_credstore"
    config:
      vendor: "constructorfabric"

  # ── Persistent credstore value store (issue #66) ──
  # This section is what decides whether secret VALUES survive a restart.
  # credstore selects exactly ONE backend plugin per vendor and the LOWEST
  # priority wins (toolkit::plugins::choose_plugin_instance), so at 50 this
  # gear displaces static-credstore-plugin (100) outright — there is no chain
  # and no fallback between the two.
  #
  # Values are sealed with AES-256-GCM under STUDIO_CREDSTORE_KEY (base64,
  # 32 bytes -> `openssl rand -base64 32`). The key is deliberately NOT in this
  # file: it arrives via the env var named by key_env below, and a literal key
  # here would travel in the image and show up in --print-config.
  #
  # Key unset      -> WARN, this gear stands down, static-credstore-plugin wins
  #                   again and you get the pre-#66 behaviour (values die with
  #                   the process) instead of a failed boot.
  # Key malformed  -> boot fails loudly, because degrading quietly would only
  #                   surface as lost secrets after the next restart.
  # Key changed    -> old rows no longer decrypt and read as absent; they are
  #                   re-sealed on the next write (bootstrap at boot, or the
  #                   user re-entering a token).
  studio-documents:
    database:
      server: "pg_main"
      dbname: "studio_documents"

  studio-credstore-pg:
    database:
      server: "pg_main"
      # Own database: the values live apart from credstore's metadata rows.
      #
      # It must EXIST before the first boot — `auto_provision` above does NOT
      # create Postgres databases (see the note next to it). A fresh volume
      # gets it from docker/initdb/01-create-databases.sql; on a volume that
      # already exists, create it once by hand:
      #   docker compose exec -T graph-postgres psql -U studio -d studio -c 'CREATE DATABASE studio_credstore_values OWNER studio;'
      dbname: "studio_credstore_values"
    config:
      vendor: "constructorfabric"         # must equal credstore.vendor above
      priority: 50                        # LOWER wins; the static plugin below is 100
      key_env: "STUDIO_CREDSTORE_KEY"

  # FALLBACK ONLY. While studio-credstore-pg above is registered, this plugin
  # loses the vendor selection and the entries below are unreachable. It stays
  # configured so a deployment with no STUDIO_CREDSTORE_KEY still boots.
  static-credstore-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100
      secrets:
        # These entries only declare the references. The VALUES are supplied
        # by studio-secrets-bootstrap from the environment on every start:
        #   export STUDIO_LLM_API_KEY=gsk_...        (Ask AI; console.groq.com
        #                                             → API Keys, no card)
        #   export STUDIO_ANTHROPIC_API_KEY=sk-ant-... (Claude Code in IDE
        #                                             sessions — not
        #                                             OpenAI-compatible, never
        #                                             goes through the proxy)
        #
        # Keep the values empty even though the whole file IS env-expanded at
        # load since #65: studio-secrets-bootstrap stays the single seeding
        # path, and a key baked into the effective config would also show up
        # in --print-config.
        - key: "openai-key"
          value: ""
        - key: "anthropic-key"
          value: ""

  mini-chat:
    database:
      server: "pg_main"
      dbname: "studio_mini_chat"
    config:
      client_credentials:
        client_id: "mini-chat"
        client_secret: "mini-chat-dev-secret"
      providers:
        # Any OpenAI-compatible /chat/completions provider; Groq's free tier
        # by default (same key as studio-llm-proxy above). Nothing in this
        # block is env-expanded — host and api_path are literals and must be
        # changed together (plain providers use /v1/chat/completions, Groq
        # nests under /openai). For OpenAI's Responses API: kind
        # openai_responses. Per-deployment overrides go through
        # APP__GEARS__MINI_CHAT__CONFIG__… env vars, which figment does read.
        openai:
          kind: openai_chat_completions
          storage_kind: openai
          host: "api.groq.com"
          api_path: "/openai/v1/chat/completions"
          auth_plugin_type: "gts.cf.core.oagw.auth_plugin.v1~cf.core.oagw.apikey.v1"
          auth_config:
            header: "Authorization"
            prefix: "Bearer "
            secret_ref: "openai-key"

  static-mini-chat-model-policy-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100
      model_catalog:
        # NB: provider_model_id is a literal model name. Keep it in sync
        # with the provider configured above / STUDIO_LLM_HOST.
        - id: "studio-default"
          provider_model_id: "llama-3.3-70b-versatile"
          display_name: "Studio assistant"
          tier: Standard
          enabled: true
          system_prompt: "You are the Constructor Studio workspace assistant. Be concise and practical."
          thread_summary_prompt: ""
          input_tokens_credit_multiplier_micro: 1000000
          output_tokens_credit_multiplier_micro: 3000000
          multimodal_capabilities: ["VISION_INPUT"]
          context_window: 128000
          max_output_tokens: 8192
          max_input_tokens: 120000
          description: "Default Studio assistant model"
          version: "2025-01"
          icon: ""
          provider_display_name: "Groq (free tier)"
          multiplier_display: "1x"
          provider_id: "openai"
          max_num_results: 10
          max_tool_calls: 2
          estimation_budgets:
            bytes_per_token_conservative: 4
            fixed_overhead_tokens: 500
            safety_margin_pct: 10
            image_token_budget: 1000
            tool_surcharge_tokens: 200
            web_search_surcharge_tokens: 500
            code_interpreter_surcharge_tokens: 1000
            minimal_generation_floor: 100
          preference:
            is_default: true
            sort_order: 1
          general_config:
            type: "chat"
            available_from: "2025-01-01T00:00:00Z"
            max_file_size_mb: 50
            api_params:
              temperature: 0.7
              top_p: 1.0
              frequency_penalty: 0.0
              presence_penalty: 0.0
              stop: []
            features:
              streaming: true
              structured_output: true
            tool_support:
              web_search: false
              file_search: false
              image_generation: false
              code_interpreter: false
              mcp: false
            supported_endpoints:
              chat_completions: true
              responses: true
              embeddings: false
              image_generation: false
              audio_speech_generation: false
              audio_transcription: false
              audio_translation: false

  # ── Studio's own gear: per-workspace Theia IDE sessions in containers ──
  # Requires a Docker daemon. The CI-published image (built by
  # .github/workflows/release.yml job `images`) is pulled/refreshed automatically —
  # `docker login ghcr.io` once if the package is private. Local hacking on
  # the image: build cf-studio-theia:latest and point `image:` at it
  # (set always_pull: false to stop the refresh from shadowing it).
  # Projects (ADR-0005 studio-project gear) retired: a project is now an AM
  # tenant (type cf.studio.tenant.project.v1~) with its attributes in the
  # cf.studio.project.config.v1~ tenant metadata — no separate project gear.

  studio-session:
    config:
      image: "ghcr.io/constructorfabric/studio-web/cf-studio-theia:edge"
      always_pull: true
      workspaces_root: "~/.cf-studio-workspaces"
      bind_host: "127.0.0.1"      # loopback only — the Theia PoC has no auth
      public_host: "localhost"
      port_range_start: 41000
      port_range_end: 41099
      max_session_secs: 14400     # 4h, then the reaper stops the container
      git_mode: "disabled"

  # ── Self-heal for config-seeded credstore secrets ──
  # Re-seeds the references declared above from the environment on every start.
  # With studio-credstore-pg active the values already survive a restart, so
  # this is normally a no-op that logs "secret accessible — no heal needed". It
  # is still the ONLY source for openai-key / anthropic-key: on a first boot,
  # after STUDIO_CREDSTORE_KEY changes (old rows stop decrypting and read as
  # absent), and whenever the persistent store stands down and the in-memory
  # fallback takes over.
  studio-secrets-bootstrap:
    config:
      secrets:
        - ref: "openai-key"
          value_env: "STUDIO_LLM_API_KEY"
          sharing: "shared"
        - ref: "anthropic-key"
          value_env: "STUDIO_ANTHROPIC_API_KEY"
          sharing: "shared"

  # ── Source connectors (driver plugins + tenant connection catalogue) ──
  # Each driver is a plugin: it registers a PluginV1 instance under
  # cf.studio.connector.plugin.v1~ and publishes a scoped ConnectorDriver
  # client. Dropping a plugin from registered_gears.rs makes that provider
  # disappear from the API — no other config changes needed.
  gitlab-connector-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  github-connector-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  bitbucket-connector-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  # Model providers. These are the credentials the IDE agents authenticate
  # with: an Anthropic key powers @theia/ai-claude-code, an OpenAI key powers
  # @theia/ai-codex. The OpenAI driver also validates any OpenAI-compatible
  # endpoint (Groq, vLLM, our own studio-llm-proxy) via the instance URL.
  anthropic-connector-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  openai-connector-plugin:
    config:
      vendor: "constructorfabric"
      priority: 100

  # Owns the connection catalogue (tenant metadata) and the REST surface.
  # Tokens live in credstore; the scope chosen on create maps onto the
  # credstore sharing mode (personal -> private, workspace -> tenant,
  # organization -> shared).
  studio-connector:
    config: {}

  # ── OpenAI-compatible LLM proxy for Theia AI inside IDE sessions ──
  # Provider-agnostic gear; this PROFILE defaults to Groq's free tier
  # (console.groq.com → API Keys; free, no card, rate-limited). Env beats
  # YAML, so any other provider is a restart away:
  #   STUDIO_LLM_BASE_URL  e.g. https://api.openai.com/v1, http://localhost:11434/v1
  #   STUDIO_LLM_MODEL     e.g. gpt-4o-mini, qwen2.5-coder
  #   STUDIO_LLM_API_KEY   the provider key (server-side only)
  # The IDE never learns the provider — it reads GET /studio-llm/v1/client-config
  # and authenticates with the user's Studio token; this gear attaches the
  # server-held key upstream.
  studio-llm-proxy:
    config:
      base_url: "https://api.groq.com/openai/v1"
      model: "llama-3.3-70b-versatile"
      developer_message_settings: "system"    # safest across providers
      api_key_env: "STUDIO_LLM_API_KEY"

  # static-authn-plugin is link-time active here too; pinned BELOW the OIDC
  # plugin so it never wins the vendor selection in this profile.
  static-authn-plugin:
    config:
      vendor: "constructorfabric"
      priority: 1000   # LOWER number wins vendor selection; oidc (100) is in charge here
      mode: static_tokens
      tokens: []

  # Knowledge graph: typed multi-tenant nodes and edges, bounded traversal and
  # hybrid retrieval. Pinned to `pg_graph` (PostgreSQL 19) by the block above;
  # it cannot run on the main server.
  graph-storage:
    database:
      server: "pg_graph"
      dbname: "graph_storage"
    config:
      # `pgq` runs each hop as a single SQL/PGQ `GRAPH_TABLE` statement. A
      # request whose scope cannot be bounded to a set of tenants is served by
      # `two_query` instead, so this is safe to leave on; `two_query` is the
      # portable fallback and `cte` is deliberately absent while the pinned
      # gears-rust revision predates the safe-CTE API it needs.
      traversal_hop: pgq
      # Embedding provider for a backend run on the host. Defaults to the
      # deterministic `fake` so `cargo run` needs no model artifacts; set
      # STUDIO_EMBEDDING_PROVIDER=onnx with STUDIO_EMBED_MODEL_PATH /
      # STUDIO_EMBED_TOKENIZER_PATH (and ORT_DYLIB_PATH) for the real model,
      # or `remote` with the STUDIO_EMBED_* endpoint settings. One provider
      # per deployment; see config/docker.yaml for the full note.
      embedding_dimension: 384
      embedding_provider: "${STUDIO_EMBEDDING_PROVIDER:-fake}"
      embedding_model_path: "${STUDIO_EMBED_MODEL_PATH:-}"
      embedding_tokenizer_path: "${STUDIO_EMBED_TOKENIZER_PATH:-}"
      embedding_remote_base_url: "${STUDIO_EMBED_BASE_URL:-https://api.openai.com/v1}"
      embedding_remote_model: "${STUDIO_EMBED_MODEL:-text-embedding-3-small}"
      embedding_remote_api_key_env: "STUDIO_EMBED_API_KEY"
